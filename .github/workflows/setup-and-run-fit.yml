name: Run FIT Tests
run-name: ${{ github.actor }} Running FIT tests on cbdinoclusters
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: 1.19
      - name: clone the FIT and couchbase-jvm-clients repo
        run: |
          git config --global user.name "littlefermatlives"
          git config --global user.email "saurabhmishra16121999@gmail.com"
          git config --get user.name
          git config --get user.email
          git clone https://github.com/couchbaselabs/transactions-fit-performer.git
